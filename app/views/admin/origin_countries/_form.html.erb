<%= form_with model: [:admin, @origin_country] do |f| %>
  <% if @origin_country.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@origin_country.errors.count, "error") %> prohibited this origin_country from being saved:</h2>

      <ul>
        <% @origin_country.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-md-6">
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: "form-control"  %>
      </div>
    </div>
  </div>

  <%= f.submit "Save", class: "btn btn-plain mt-3" %>
<% end %>
