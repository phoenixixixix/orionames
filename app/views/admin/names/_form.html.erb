<%= form_with(model: admin_name) do |form| %>
  <% if admin_name.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_name.errors.count, "error") %> prohibited this admin_name from being saved:</h2>

      <ul>
        <% admin_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
