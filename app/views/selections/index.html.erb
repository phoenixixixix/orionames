<% content_for :meta_title, "#{t ".heading"} | О.Імена" %>
<% content_for :meta_description, "Підбірки імен міститять інформацію для тих, хто шукає нестандартні та рідкісні імена, які можуть допомогти виділитися серед інших. Топ імен в україні та світі." %>

<h1 class="page-heading"><%= t ".heading" %></h1>

<div class="row row-cols-md-2">
  <%= render partial: "selection", collection: @selections %>
</div>

<%= paginate @selections, window: 1 %>
